Lordandtaylor-parser
=========

   for run use:
   
    >> pip install -r requierments.txt
  
   in three terminals:
   
    >> ./manage.py runserver
    >> celery -A django_parse_proj worker
    >> scrapy crawl landt
